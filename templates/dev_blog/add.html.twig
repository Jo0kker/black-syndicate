{% extends "base.html.twig" %}

{% block body %}
    {% include "partials/navBar.html.twig" %}
    <div class="container mt-3">
        {{ form_start(form) }}
        <div class="">
            <label for="register-form-name">Titre :</label>
            {{ form_errors(form.title) }}
            {{ form_widget(form.title) }}
        </div>
        <div class="">
            <label for="register-form-name">Url de l'image :</label>
            {{ form_errors(form.img) }}
            {{ form_widget(form.img) }}
        </div>
        <div class="">
            <label for="register-form-password">Description :</label>
            {{ form_errors(form.description) }}
            {{ form_widget(form.description) }}
        </div>
        <div class="">
            <label for="register-form-repassword">Détail :</label>
            {{ form_errors(form.detail) }}
            {{ form_widget(form.detail) }}
        </div>
        <div class="col_full nobottommargin mt-3 mb-3 text-center">
            <button class="button button-3d button-black nomargin" id="register-form-submit" name="register-form-submit" value="register">Enregistrer l'article</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}